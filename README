# GTFS to MySQL
The easiest node.js script to import GTFS data into MySQL

#### Setup
Download all dependencies
```console
npm install
```

Then use the create_db.sql to create/replace the database
```console
cat create_db.sql | mysql -u -p root
```

Last step is to run the node.js script, all you have to do is
```console
npm run import
```