GTFS to MySQL
The easiest node.js script to import GTFS data into MySQL

1. Download all dependencies
npm install

2. Use create_db.sql to create/replace the database
cat create_db.sql | mysql -u -p root

3. Run the node.js script to import
npm run import
